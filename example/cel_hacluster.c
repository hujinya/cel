#include "cel/net/hacluster.h"
#include "cel/error.h"

int hacluster_test(int argc, TCHAR *argv[])
{
  /*  CelHa ha;

    if (cel_hacluster_init(&ha) != 0)
    {
        _tprintf(_T("%s\r\n"), cel_geterrstr(cel_geterrno()));
        return 1;
    }
    while (TRUE)
        usleep(10 * 1000);*/

    return 0;
}
